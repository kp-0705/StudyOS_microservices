---
- name: Deploy Kubernetes Manifests
  hosts: localhost
  connection: local
  gather_facts: false
  
  vars_files:
    - "../group_vars/all/vault.yml"

  tasks:
    - name: Include k8s_deploy role
      include_role:
        name: k8s_deploy
