# ============================================================
# Deployment: mongodb
# StatefulDB deployment for StudyOS using official mongo:6 image
# Uses PVC for persistent data storage at /data/db
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: studyos
  labels:
    app: studyos
    tier: database
    component: mongodb
spec:
  replicas: 1         # Only 1 replica (MongoDB is stateful)
  selector:
    matchLabels:
      app: mongodb

  # ---- Strategy: Recreate ----
  # Singleton databases with PVCs MUST use Recreate to ensure the old pod 
  # releases the storage lock before the new pod starts.
  strategy:
    type: Recreate

  template:
    metadata:
      labels:
        app: mongodb
        tier: database
    spec:
      containers:
        - name: mongodb
          image: mongo:6.0
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 27017

          # ---- Volume Mount (/data/db is where MongoDB stores data) ----
          volumeMounts:
            - name: mongodb-storage
              mountPath: /data/db

          # ---- Resource Limits & Requests ----
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"

          # ---- Liveness Probe (Is MongoDB alive?) ----
          livenessProbe:
            exec:
              command:
                - mongosh
                - --eval
                - "db.adminCommand('ping')"
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3

          # ---- Readiness Probe (Is MongoDB ready to accept connections?) ----
          readinessProbe:
            exec:
              command:
                - mongosh
                - --eval
                - "db.adminCommand('ping')"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 15
            successThreshold: 1
            failureThreshold: 5

      # ---- PVC Volume ----
      volumes:
        - name: mongodb-storage
          persistentVolumeClaim:
            claimName: mongodb-pvc
